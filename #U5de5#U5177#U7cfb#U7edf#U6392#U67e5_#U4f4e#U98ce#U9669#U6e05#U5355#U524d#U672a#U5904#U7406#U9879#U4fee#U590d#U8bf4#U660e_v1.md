# 工具系统排查 · "低风险清单"之前未处理项修复说明

对应报告：《工具系统全面排查整合报告》第二节"真实缺口" + 第三节"报告提到但从未排进修复计划、代码里确认仍未处理"。
本轮只处理这两节列出的 6 项，第三节末尾提到的低风险清单（#37/39-51/53-58/60/62-64，约20项）尚未逐条验证，不在本轮范围内。

---

## 1. FileReadTool 二进制防护补第三层（`BuiltinTools.kt`）

**问题**：原实现只有扩展名黑名单 + 文件大小限制两层，没有二进制内容探测。改后缀成
`.txt`、用 `.bin`/`.dat`/无后缀等未列入黑名单的二进制文件，仍会漏进
`bufferedReader` 通用文本读取路径，理论上仍可能 OOM。

**修复**：在扩展名黑名单和大小限制之后、正式走文本读取路径之前，加第三层——读取
文件前 8KB，探测是否含 null 字节，命中则判定为二进制内容直接拒绝。三层防护现已
与报告原方案（P0-1）完全对齐。

## 2. RuleDistillTool try-catch 覆盖补全（`AgentCoreTools.kt`，对应 #30）

**问题**：522-561 行"数据库查询未包裹 try-catch"，实际代码只包了
`goalRepo.getById()` 一处，`memoryRepo.countLockedRules()` 和
`memoryRepo.getRulesByGoal()` 仍在 try 外。外层 ToolCallInterceptor/WorkflowEngine
的 catch-all 能兜住不真崩，但报错信息不友好。

**修复**：把这两条查询一并纳入 try-catch，失败时返回统一的
`toolFailure(..., "rule_count_failed", e)`，与 `goalRepo.getById()` 的覆盖方式保持一致。

## 3. #25 EmailTools 邮件正文解析失败静默为空（`EmailTools.kt`）

**问题**：`extractTextPreview` 解析失败时 catch 住异常返回空字符串，和"邮件本身
没有正文"这种正常情况表现完全一样——报告里都是"没有摘要那一行"，用户无法区分。

**修复**：加了哨兵值 `PREVIEW_PARSE_FAILED`，解析失败时返回该哨兵而非空字符串；
调用处据此显式展示"（正文解析失败，无法预览，可能是不支持的邮件格式）"，与真正
无正文的邮件区分开。

## 4. #28 SystemSuggestionGenerator.buildHistorySummary 无长度上限（`SystemSuggestionGenerator.kt`）

**问题**：`getUnmerged()` 取出的全部未合并阶段摘要无条数限制地拼接成
`historySummary`，单条摘要虽然各自截断，但摘要总数不受控——长期不触发合并周期时
持续累积，可能导致拼出的文本超出 LLM 单次调用输入长度限制，调用失败。

**修复**：加 `MAX_DIGESTS_IN_SUMMARY = 20` 常量，取 `getUnmerged()` 结果（按
`createdAt ASC`）的尾部最新 20 条，代表"最近活跃情况"样本，其余不再拼入。

## 5. #29 IdentityPromotionEvaluator.executePromotion 静默 return（`IdentityPromotionEvaluator.kt` + `SpecialtyEvolutionViewModel.kt`）

**问题**：`profile` 为 null（如对应专长档案已被删除）时函数直接 `return`，
返回类型是 `Unit`；调用方 `SpecialtyEvolutionViewModel.confirmPromotion` 不管
执行结果如何都固定弹出"已写入她的人设核心"的成功提示，用户会看到"成功"提示但
实际什么都没变，造成困惑。

**修复**：`executePromotion` 改为返回 `Boolean`——profile 不存在或执行异常时返回
`false`（并补了一条 `ZLog.w` 说明跳过原因），成功返回 `true`。调用方据实际结果
分流提示语：成功仍是原提示，失败则改为"晋升未能完成，这条建议对应的专长档案
可能已被删除，或处理时出现了问题"。

## 6. #36 GitCommitPushTool 多文件提交无原子性/失败不提示已提交文件（`GitCommitPushTool.kt`）

**问题**：`pushFiles` 用 GitHub Contents API 逐文件 PUT（每个文件各自是一次独立
commit，API 本身不支持多文件原子提交），中途某个文件失败时异常直接从
`pushFiles` 抛出，`execute()` 的 catch 块只返回一句通用的
`"git_commit_push_failed"`，完全不提示前面已经成功提交的文件——LLM/用户据此
重试整个 `files_json`，会给已成功的文件重复生成一次 commit。

**修复**：加了 `PartialCommitException`，携带"已成功提交的路径列表 + 失败文件 +
最后一次成功的 commit sha + 原因"。`pushFiles` 在每个文件 PUT（含前置的
`getFileSha` 查询）失败时都用这个异常带出已完成的进度，`execute()` 单独 catch
它，拼出准确的错误信息（列出已成功文件 + 最后 commit sha + 提示"重试时只重新
提交失败及之后的文件"）。

**说明**：GitHub Contents API 本身的限制决定了做不到真正的多文件原子提交
（需要改用 Git Data API 的 blob/tree/commit 三步流程才行，属于更大的改造，这次
没做）。这次解决的是报告里点名的问题——"失败了但不知道已经提交到哪了"这个信息
缺失，不是把提交机制改成真原子的。

---

## 改动文件清单

- `data/agent/BuiltinTools.kt` — FileReadTool 三层防护
- `data/agent/AgentCoreTools.kt` — RuleDistillTool try-catch 补全
- `data/agent/EmailTools.kt` — 邮件预览解析失败哨兵值
- `data/agent/SystemSuggestionGenerator.kt` — buildHistorySummary 条数上限
- `data/agent/IdentityPromotionEvaluator.kt` — executePromotion 返回 Boolean
- `data/agent/GitCommitPushTool.kt` — PartialCommitException 部分提交进度
- `ui/viewmodel/SpecialtyEvolutionViewModel.kt` — confirmPromotion 据结果分流提示语

只编辑现有文件，未新增/删除文件，`.kt` 文件总数保持 417（与本轮解压时一致）。

## 未做的验证

本环境没有 kotlinc / gradle / Android SDK，且网络关闭无法拉取 Gradle 依赖，因此
没能跑真实编译。已做的自检：
- 全部改动文件括号（`{}`/`()`）配平检查（`BuiltinTools.kt` 存在原文件已有的
  -1 偏移，确认源于原有字符串/注释内容，非本轮改动引入，本轮改动前后偏移量
  一致增量平衡）
- 逐处人工审查改动上下文（尤其是 `executePromotion` 返回类型变更后的调用方
  同步、`PartialCommitException` 的抛出/捕获路径）

建议接下来在实际 Android Studio / gradle 环境跑一次编译确认。

## 未处理范围（按你的说明，本轮不做）

低风险清单（#37/39-51/53-58/60/62-64，约20项）——两版报告都注明"健壮性很低，
可顺手处理不需要单独排期"，尚未逐条验证，留待下一轮。
