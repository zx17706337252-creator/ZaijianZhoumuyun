# 工具系统排查 · "低风险清单"第一组/第二组已验证项修复说明

对应文档：《工具系统全面排查整合报告》"低风险清单"第一组（#37/39/40/41/42）、
第二组（#43/44/45/46/47）核实结果。本轮只处理这两组共 10 项中"确认存在"的部分，
第三组（#48/49/50/51/53）及之后未验证，不在本轮范围内。

---

## 已修复项

### #41 ScheduleCreateTool.kt / #43 ScheduleListTool.kt — Long 溢出（真实风险）

**问题**：`parseHoursOrError` 只挡负数，无上限。极大的 `interval_hours` /
`delay_hours` / `hours_ahead` 经 `.toLong()` 会先钳位到 `Long.MAX_VALUE`，
紧接着 `System.currentTimeMillis() + 那个值` 这步普通 Long 加法会真正
wraparound 成巨大负数，`nextRunAt` / 查询窗口可能变成远早于当前时间的负值。

**修复**：`ScheduleToolParamUtil` 加统一上限 `MAX_HOURS = 87600`（10年），
`parseHoursOrError` 超过即报错。`ScheduleListTool` 的 `hours_ahead` 原先是
独立的一份校验逻辑，改为直接复用 `parseHoursOrError`（与 #41 同源同修），
不再各自维护一份。`ScheduleUpdateTool` 因本就调用同一个共享函数，自动一并修复。

### #44 ScheduleToolParamUtil.kt — JSON null 被转成字符串 "null"

**问题**：`obj.get(key).toString()` 遇到 JSON 显式 `null` 时，org.json 用
`JSONObject.NULL` 哨兵表示，其 `toString()` 恰好是字符串 `"null"`，没有特判。

**修复**：显式判断 `value === JSONObject.NULL` 时跳过该 key（等同于"未传该
参数"），因为 `Map<String, String>` 本身无法表达真正的 null，这是比塞一个
容易被误读的字符串 `"null"` 更安全的语义。

### #45 HeartbeatSetTool.kt — mkdirs() 返回值未检查

**问题**：目录创建失败时不会立即报错，而是让后续 `writeText` 抛出普通异常，
被外层 catch 转成统一的失败提示，看不出根因。

**修复**：显式检查 `mkdirs()` 返回值，仅在目录确实仍不存在（排除"已存在"和
"并发下已被创建好"两种正常情况）时抛出带明确路径的异常。

### #46 HeartbeatUpdateTool.kt — 空文件解析错误信息不清晰

**问题**：清单文件存在但内容为空时，`JSONObject(...)` 抛异常，落入通用
catch，只返回"更新心跳清单失败，请稍后重试"，看不出真实原因。

**修复**：读取文件内容后先判断 `isBlank()`，命中则直接返回明确提示——
"心跳清单文件为空（可能未正确初始化或已损坏），请先用 heartbeat_set 重新写入清单"。

### #47 SkillTools.kt — usageCount 展示基于陈旧快照

**问题**：数据库层 `incrementUsage` 走原子 SQL，计数本身没问题；但展示用的是
调用前读到的 `existing.usageCount + 1` 现算值，并发场景下可能比实际值小。

**修复**：`recordUsage()` 落库后重新 `repo.getById(skillId)` 查一次最新记录，
用真实计数展示，不再自己推算。

### #39 AgentMetaTools.kt — 顺手修复报告点出的小瑕疵（原判定"已修复"）

**问题**：原 1MB 上限按字符数（`CharArray`）计数，UTF-8 多字节字符下实际
字节量最坏可达标称上限的约 4 倍，不是精确的字节上限。

**修复**：改为按原始字节流（`ByteArray` + `InputStream`）读取计数，精确控制
在 `MAX_RESPONSE_BYTES` 以内，读够/读完后再统一解码为字符串。

---

## 未改动项（核实结果与原报告一致，无需处理）

- **#37** FileSystemTools.kt — 已修复，且该风险本就只在 Windows JVM 成立，
  Android 运行环境原本就不受影响，属于防御纵深，无需进一步处理。
- **#40** AgentMetaTools.kt — 已修复，逻辑正确，无需处理。
- **#42** ScheduleUpdateTool.kt — 核实为当前安全（泛型擦除警告，运行时 cast
  必定成立），无实际风险，无需处理。

---

## 改动文件清单

- `data/agent/AgentMetaTools.kt` — p3HttpGet 改字节流精确计数（#39 复核补丁）
- `data/agent/ScheduleToolParamUtil.kt` — parseHoursOrError 加上限（#41/#43）+ JSON null 处理（#44）
- `data/agent/ScheduleListTool.kt` — hours_ahead 改用共享校验函数（#43）
- `data/agent/HeartbeatSetTool.kt` — mkdirs() 返回值检查（#45）
- `data/agent/HeartbeatUpdateTool.kt` — 空文件明确错误提示（#46）
- `data/agent/SkillTools.kt` — usageCount 展示改为落库后重查（#47）

只编辑现有文件，未新增/删除文件，`.kt` 文件总数保持 417（与解压时一致）。

## 未做的验证

本环境没有 kotlinc / gradle / Android SDK，网络关闭无法拉取 Gradle 依赖，
未能跑真实编译。已做的自检：全部改动文件括号（`{}`/`()`）配平检查（逐文件
用脚本核对改动前后差值为 0）；逐处人工审查改动上下文（尤其是 `ScheduleListTool`
改为复用共享函数后的调用方式、`SkillTools.kt` 中 `updated`/`existing` 两个
对象的取用范围）。建议接下来在实际 Android Studio / gradle 环境跑一次编译确认。

## 未处理范围（本轮不做）

低风险清单第三组及之后（#48/49/50/51/53...，约10余项）尚未逐条验证，留待下一轮。
